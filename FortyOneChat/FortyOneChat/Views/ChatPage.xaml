<ContentPage
    x:Class="FortyOneChat.Views.ChatPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    prism:ViewModelLocator.AutowireViewModel="True">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Thickness x:Key="pageMargin">10</Thickness>
            <Thickness x:Key="contentMargin">5</Thickness>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Padding="{StaticResource pageMargin}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="8*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="8*" />
                    <RowDefinition Height="2*" />
                </Grid.RowDefinitions>

                <!--  Message cards  -->
                <StackLayout
                    Grid.Row="0"
                    Grid.Column="0"
                    Padding="{StaticResource contentMargin}">
                    <ScrollView>
                        <Label>
                            Chatting...
                        </Label>
                    </ScrollView>
                </StackLayout>

                <!--  Online list  -->
                <StackLayout
                    Grid.Row="0"
                    Grid.Column="1"
                    Padding="{StaticResource contentMargin}">
                    <Label
                        FontSize="Large"
                        HorizontalTextAlignment="Center"
                        Text="{Binding Path='OnlineUserCollection.Count' StringFormat='Online {0}'}"
                        TextColor="Green"
                        VerticalTextAlignment="Center" />
                    <ListView ItemsSource="{Binding OnlineUserCollection}">

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <ViewCell.View>
                                        <Label
                                            BackgroundColor="#CCFF90"
                                            HorizontalTextAlignment="Center"
                                            Text="{Binding Name}"
                                            TextColor="Black"
                                            VerticalTextAlignment="Center" />
                                    </ViewCell.View>
                                </ViewCell>

                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>

                <!--  Message entry  -->
                <StackLayout
                    Grid.Row="1"
                    Grid.Column="0"
                    Padding="{StaticResource contentMargin}">
                    <Editor Text="{Binding NewMessage}" />
                </StackLayout>

                <!--  Send button  -->
                <StackLayout
                    Grid.Row="1"
                    Grid.Column="1"
                    Padding="{StaticResource contentMargin}">
                    <Button
                        Command="{Binding SendMessageCommand}"
                        HorizontalOptions="Center"
                        Text="Send"
                        VerticalOptions="CenterAndExpand" />
                </StackLayout>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>